# IPython log file

anything = "This is but a test, this is just a trial run. I still can't push reset, trade it in for a better one."
cesar={}
for i in range(0,26):
    cesar[chr(i+65)]=chr(((i+12)%26)+65)
    cesar[chr(i+97)]=chr(((i+12)%26)+97)
    
shift = anything
for c in shift:
    if ord(c) in range (97,122) or ord(c) in range (65, 90):
        shift.replace(c, cesar[c])
        
get_ipython().magic(u'save')
get_ipython().magic(u'save (1,5)')
get_ipython().magic(u'save (5)')
get_ipython().magic(u'logsave')
get_ipython().magic(u'logstart')
anything = "This is but a test, this is just a trial run. I still can't push reset, trade it in for a better one."
shift = anything
cesar={}
for i in range(0,26):
    cesar[chr(i+65)]=chr(((i+12)%26)+65)
    cesar[chr(i+97)]=chr(((i+12)%26)+97)
    
for c in shift:
    if ord(c) in range (97,122) or ord(c) in range (65, 90):
        shift.replace(c, cesar[c])
        
get_ipython().magic(u'pastebin 11-15')
ord(.
ord(.)
ord('.')
chr(46)
chr(30)
chr(40)
chr(39)
chr(38)
chr(36)
chr(35)
chr(34)
chr(32)
chr(31)
chr(32)
chr(32)
for i in range(32,60):
    print i
    
for i in range(32,60):
    print chr(i)
    
for i in range(32,62):
    print chr(i)
    
for i in range(32,77):
    print chr(i)
    
for i in range(32,67):
    print chr(i)
    
for i in range(32,65):
    print chr(i)
    
anything = "This is but a test, this is just a trial run. I still can't push reset, trade it in for a better one."
shift = anything
cesar={}
for i in range(0,26):
    cesar[chr(i+65)]=chr(((i+12)%26)+65)
    cesar[chr(i+97)]=chr(((i+12)%26)+97)
    
for c in shift:
    if ord(c) in range (97,122) or ord(c) in range (65, 90):
        shift.replace(c, cesar[c])
        
shift
for c in shift:
    if ord(c) in range (97,122) or ord(c) in range (65, 90):
        shift=shift.replace(c, cesar[c])
        
shift
shift
cesar
for c in shift:
    if ord(c) in range (97,122) or ord(c) in range (65, 90):
        shift=shift.replace(c, cesar[c])
        
shift
for i in range(0,26):
    cesar[chr(i+65)]=chr(((i+13)%26)+65)
    cesar[chr(i+97)]=chr(((i+13)%26)+97)
    
shift = anything
for c in shift:
    if ord(c) in range (97,122) or ord(c) in range (65, 90):
        shift=shift.replace(c, cesar[c])
        
shift
for c in shift:
    if ord(c) in range (97,122) or ord(c) in range (65, 90):
        shift=shift.replace(c, cesar[c])
        
shift
cesar
shift
immpot this
import this
this.s
cesar
cesar.keys()
cesar.keys()
'a' in cesar.keys()
';' in cesar.keys()
for c in shift:
    if c in cesar.keys():
        shift=shift.replace(c, cesar[c])
        
shift=anything
for c in shift:
    if c in cesar.keys():
        shift=shift.replace(c, cesar[c])
        
shift
shift=anything
shift
for c in shift:
    if c in cesar:
        shift=shift.replace(c, cesar[c])
        
shift
for c in shift:
    if c in cesar:
        shift=shift.replace(c, cesar[c])
        
shift
for c in shift:
    if c in cesar:
        shift=shift.replace(c, cesar[c])
        
shift
shift
cesar
shift
anything
for c in shift:
    if c in cesar:
        shift=shift.replace(c, cesar[c])
        
exit()
